#include <stdio.h>
#include <stdlib.h>

int main()
{
   int r = 6;
   int h = 10;

    float pi = 3.14;
    printf(" the  area of  cicrcle with  radius %d is  %f",r,pi*r*r);
    printf("The volume  of  cylinder with radius %d h %d is  %f,"r,h,pi*r*r*h);

    return 0;
}
